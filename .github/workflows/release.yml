name: Release

on:
  push:
    branches:
      - "release/**"

permissions:
  contents: write
  issues: write
  pull-requests: write
  actions: read

jobs:
  prepare-release:
    runs-on: ubuntu-latest
    outputs:
      current-version: ${{ steps.get-version.outputs.version }}
      release-branch: ${{ steps.branch-info.outputs.branch }}
    steps:
      - uses: actions/checkout@v4

      - name: Get branch info
        id: branch-info
        run: |
          echo "branch=${GITHUB_REF#refs/heads/}" >> $GITHUB_OUTPUT

      - name: Get current version
        id: get-version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Current version: $VERSION"

  request-bump-type:
    needs: prepare-release
    runs-on: ubuntu-latest
    steps:
      - name: Request version bump type
        uses: actions/github-script@v7
        with:
          script: |
            const { data: workflow } = await github.rest.actions.getWorkflowRun({
              owner: context.repo.owner,
              repo: context.repo.repo,
              run_id: context.runId
            });

            const issue = await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `🚀 Release ${workflow.head_branch} - Select Version Bump`,
              body: `
            ## Release Request for \`${workflow.head_branch}\`

            Current version: **${{ needs.prepare-release.outputs.current-version }}**

            Please select the type of version bump for this release:

            **Comment with one of the following:**
            - \`/release patch\` - for bug fixes (e.g., 1.0.0 → 1.0.1)
            - \`/release minor\` - for new features (e.g., 1.0.0 → 1.1.0)  
            - \`/release major\` - for breaking changes (e.g., 1.0.0 → 2.0.0)

            ---
            **Workflow Run:** [#${{ github.run_number }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
            **Branch:** \`${{ needs.prepare-release.outputs.release-branch }}\`
              `,
              labels: ['release', 'needs-input']
            });

            console.log(`Created issue #${issue.data.number} for release approval`);

  release:
    runs-on: ubuntu-latest
    if: github.event_name == 'issue_comment' && contains(github.event.comment.body, '/release')
    steps:
      - name: Check if commenter has permission
        uses: actions/github-script@v7
        with:
          script: |
            const { data: collaborator } = await github.rest.repos.getCollaboratorPermissionLevel({
              owner: context.repo.owner,
              repo: context.repo.repo,
              username: context.actor
            });

            if (!['admin', 'write'].includes(collaborator.permission)) {
              throw new Error(`User ${context.actor} does not have permission to trigger releases`);
            }

      - name: Parse bump type
        id: parse-bump
        uses: actions/github-script@v7
        with:
          script: |
            const comment = context.payload.comment.body;
            const match = comment.match(/\/release\s+(patch|minor|major)/);

            if (!match) {
              throw new Error('Invalid release command. Use: /release patch, /release minor, or /release major');
            }

            const bumpType = match[1];
            console.log(`Parsed bump type: ${bumpType}`);
            core.setOutput('bump-type', bumpType);

      - name: Get release branch from issue
        id: get-branch
        uses: actions/github-script@v7
        with:
          script: |
            const issue = context.payload.issue;
            const branchMatch = issue.body.match(/\*\*Branch:\*\* `([^`]+)`/);

            if (!branchMatch) {
              throw new Error('Could not extract branch from issue');
            }

            const branch = branchMatch[1];
            console.log(`Found branch: ${branch}`);
            core.setOutput('branch', branch);

      - uses: actions/checkout@v4
        with:
          ref: ${{ steps.get-branch.outputs.branch }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build project
        run: bun run build

      - name: Test (placeholder)
        run: |
          echo "🧪 Running tests..."
          echo "✅ Tests would run here"
          # Add your actual test command here when ready
          # bun test

      - name: Get current version for tagging
        id: current-version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Current version for tagging: $VERSION"

      - name: Configure git
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"

      - name: Create tag from current version
        run: |
          TAG="v${{ steps.current-version.outputs.version }}"
          echo "Creating tag: $TAG"
          git tag "$TAG"
          git push origin "$TAG"

      - name: Bump version
        id: bump-version
        run: |
          echo "Bumping version with type: ${{ steps.parse-bump.outputs.bump-type }}"
          bun version --${{ steps.parse-bump.outputs.bump-type }}
          NEW_VERSION=$(node -p "require('./package.json').version")
          echo "new-version=$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "New version: $NEW_VERSION"

      - name: Commit version bump
        run: |
          git add package.json
          git commit -m "chore: bump version to ${{ steps.bump-version.outputs.new-version }}"
          git push origin ${{ steps.get-branch.outputs.branch }}

      - name: Setup Node.js for npm publish
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          registry-url: "https://registry.npmjs.org"

      - name: Publish to npm
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Close release issue
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.issue.number,
              body: `✅ **Release completed successfully!**
              
            - **Tag created:** \`v${{ steps.current-version.outputs.version }}\`
            - **New version:** \`${{ steps.bump-version.outputs.new-version }}\`
            - **Published to npm:** ✅
            - **Branch:** \`${{ steps.get-branch.outputs.branch }}\`

            The package has been published to npm with version \`${{ steps.bump-version.outputs.new-version }}\`.`
            });

            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.issue.number,
              state: 'closed',
              labels: ['release', 'completed']
            });
